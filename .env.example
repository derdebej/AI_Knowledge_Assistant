# Used by docker-compose.yml for the postgres service.
# Copy to .env and adjust if desired; defaults match backend/.env.example's DATABASE_URL.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ai_knowledge_assistant
